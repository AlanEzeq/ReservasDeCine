@model Sala

@{
    ViewData["Title"] = $"Modificar Sala {Model.Numero}";
}

<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Edición de Sala</h5>
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="Id" />
                    <div class="form-group">
                        <label asp-for="Numero" class="control-label"></label>
                        <input asp-for="Numero" class="form-control" />
                        <span asp-validation-for="Numero" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="CapacidadButacas" class="control-label"></label>
                        <input asp-for="CapacidadButacas" class="form-control" />
                        <span asp-validation-for="CapacidadButacas" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label for="TipoSalaId" class="control-label">Tipo de Sala</label>
                        <select name="TipoSalaId" class="form-control selectpicker" asp-items="ViewBag.TipoSalaId" ></select>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Confirmar" class="btn btn-success" />
                        <a asp-action="Index" class="btn btn-danger">Cancelar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}